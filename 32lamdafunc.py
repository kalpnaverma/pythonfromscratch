# lamda finctions are single expression
# without lamda 
# def double(x):
#     return x*2
# with lamda both  expression are same 
double=lambda x: x*2  #single line expression
print(double(5))
cube=lambda x: x*x*x
print(cube(6))
avg=lambda  s,y,r:(s+y+r)/3
print(avg(4,5,2))
# use lamda function for small task or one line task not convenient for big task
